/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // ✏️ Ajoutez ici les domaines de vos images externes
    // ex: domains: ['votre-cdn.com']
    remotePatterns: [],
  },
}

module.exports = nextConfig
